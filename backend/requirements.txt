# AttendAI v4 Backend — Railway deployment
# No face_recognition/dlib/YOLO — CV runs on HuggingFace Spaces

# ── Django ──────────────────────────────────────────────────────────
Django==4.2.9
djangorestframework==3.14.0
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.3.1
channels==4.0.0
channels-redis==4.1.0
daphne==4.0.0

# ── Database ────────────────────────────────────────────────────────
dj-database-url==2.1.0       # parses DATABASE_URL from Railway PostgreSQL
psycopg2-binary==2.9.9       # PostgreSQL adapter

# ── LLM (Groq — free, fast) ─────────────────────────────────────────
groq==0.9.0

# ── HTTP client (for CV service communication) ───────────────────────
httpx==0.27.0
requests==2.31.0

# ── Utilities ────────────────────────────────────────────────────────
Pillow==10.2.0
python-dotenv==1.0.1
whitenoise==6.7.0            # serve static files on Railway

# ── Minimal CV (for image saving only, no model inference) ───────────
opencv-python-headless==4.9.0.80
numpy>=1.26.4,<2.0
